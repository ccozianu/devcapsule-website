---
status: reported
severity: untriaged
target: none
owner: maintenance
opened: YYYY-MM-DD
requirements: []
---

# Bug: Short Title

Fields above are controlled; see *Bug Intake* in `WORKFLOW.md`. `status` is
one of reported, confirmed, fixing, fixed, closed, retired. `severity` is one
of blocking, major, minor, untriaged. `owner` is the open workstream whose goal
covers the bug, otherwise `maintenance`; `none` in single-stream mode.

## Symptom

What the user or agent observed.

## Reproduction

1. ...

Expected:

Actual:

## Evidence

Logs, stack traces, commands, and timestamps. Do not include secrets.

## Close Criteria

Done means:

Verification:

Reopen if:
